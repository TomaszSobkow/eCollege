package topic10;import java.io.File;import java.io.FileWriter;import java.io.IOException;import java.time.LocalTime;import java.util.Scanner;public class ScannerTes {    static String dataFile = "";public static void readTestFile(){    Scanner scannerReadTestFile = new Scanner("password.txt");    while (scannerReadTestFile.hasNext()){        dataFile = dataFile.concat(scannerReadTestFile.nextLine());    }    System.out.println("Thanks");}    public static void main(String args[])  throws IOException {        Scanner scannerReadTestFile = new Scanner(new File("test.txt"));        FileWriter writeToNewFile = new FileWriter("newFile.txt", true);        while (scannerReadTestFile.hasNext()){            dataFile = dataFile.concat(scannerReadTestFile.nextLine()+"\n");        }        System.out.println("In string file \n".toUpperCase()+dataFile);//        if(dataFile.contains("1")){//           dataFile = dataFile.substring(0,0).concat(dataFile);//            writeToNewFile.write(dataFile);//            writeToNewFile.close();            //System.out.println(dataFile);        }}